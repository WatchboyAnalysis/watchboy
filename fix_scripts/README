Run 'make clean && make' first to make sure everything is up to date

Run python batch.py file1.root file2.root ...

For older simplified files run: simpfix file1.root file2.root ...

To build the gate structure run: gate_builder file1.root file2.root ...